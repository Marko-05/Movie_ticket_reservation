Version 2: \nOverhauled a large portion of the app including the design, as well as some of the functionality. The main change is updating the seat picking form so that it dynamically creates all the seats, removing the redundant saveFileDialog and using a json file to track which seats are already reserved instead. Also changed how the app shutdown process works so that one confirmation is enough to shut down.
